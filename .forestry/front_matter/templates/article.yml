---
label: Article
hide_body: false
display_field: title
fields:
- type: text
  name: title
  label: title
  config:
    required: true
- name: hero_image
  type: file
  config:
    maxSize: '10'
  label: hero image
  description: Enter hero image for article here
  default: ''
- name: latitude
  type: number
  label: latitude
  description: The latitude of the associated location
  default: 0
  required: true
  config:
    min: 
    max: 
    step: 
    required: true
- name: longitude
  type: number
  label: longitude
  description: The longitude of the associated location
  default: 0
  required: true
  config:
    min: 
    max: 
    step: 
    required: true
- name: apposition
  type: select
  default: situation
  config:
    required: true
    options:
    - situation
    - solution
    source:
      type: simple
      section: 
      file: 
      path: 
  label: apposition
  description: Indicates which one of several ways the article is apposite to the
    issue of homelessness
- name: is_published
  type: boolean
  label: is published
  description: If true, the article will be included on the website; if false the
    article will be omitted.
  default: false
pages:
- content/articles/renting-in-oxford.why-the-figures-just-don-t-add-up.md
