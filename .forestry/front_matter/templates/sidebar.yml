---
label: Sidebar
hide_body: false
fields:
- name: title
  type: text
  config:
    required: false
  label: title
  description: This title goes in the sidebar header
- name: legend_text
  type: textarea
  default: ''
  config:
    required: false
    wysiwyg: true
    schema:
      format: markdown
  label: legend text
  description: This text goes below the legend icons
- name: button_url
  type: text
  config:
    required: true
  label: button URL
  description: Indicates where the footer button should link to. This should be an
    URL (like https://example.com/) or a local path (like /map/some-page-slug)
pages:
- content/data/sidebar.md
